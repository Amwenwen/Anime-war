/**
 * env.js — Override this file to point the client at your game server.
 * In development: server runs on localhost:3000 (auto-detected).
 * In production: set GAME_SERVER_URL to your deployed server URL.
 *
 * Example for Railway:
 *   window.GAME_SERVER_URL = 'https://anime-war-production.up.railway.app';
 *
 * Example for Render:
 *   window.GAME_SERVER_URL = 'https://anime-war.onrender.com';
 *
 * Leave as null to use same-origin (works when client + server are on same host).
 */
window.GAME_SERVER_URL = null;
